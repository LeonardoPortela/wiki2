/// <reference types="node" />
import { Transform } from 'stream';
import { SearchRequester } from '../search-requester';
import { SearchOptions } from '../types';
export declare class IndexStream extends Transform {
    private requester;
    private index;
    private options?;
    private buffer;
    constructor(requester: SearchRequester, index: string, options?: SearchOptions);
    private request;
    private process;
}
